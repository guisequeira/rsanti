export function easeOutQad (duration) {
 return `${duration}ms cubic-bezier(0.250, 0.460, 0.450, 0.940)`;
}

export function easeOutExpo (duration) {
  return `${duration}ms cubic-bezier(0.190, 1.000, 0.220, 1.000)`;
}

