import { MovingLetterDirective } from './moving-letter.directive';

describe('MovingLetterDirective', () => {
  it('should create an instance', () => {
    const directive = new MovingLetterDirective();
    expect(directive).toBeTruthy();
  });
});
